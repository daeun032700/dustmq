v = input("입력값: ")

#만약에 입력 받은 값이 0 이면 0이다
if v == "0":
    print("0이다")
#만약에 입력 바든 값이 0 이 아니면 0이 아니다
if v != 0:
    print("0d이 아니다")
#만약에 입력받은 값이 0이면(0과 같다면) 0이다를 출력 아니면 0이 아니다 출력
if v == 0:
    print("0이다")
else print